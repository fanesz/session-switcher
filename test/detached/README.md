# README

Tests in this folder do not test the source code but copy/paste aspects of it to test in isolation.

It is not safe to assume that passing tests in this folder validate the code in the `src` directory.

Run `npm run test` to run all Jest tests.